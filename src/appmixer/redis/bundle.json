{
    "name": "appmixer.redis",
    "version": "1.0.0",
    "changelog": {
        "1.0.0": [
            "Initial version with Redis operations: Get, Set, Delete, Increment, Info, Keys, Push, Pop, List Length, Publish",
            "Support for Redis data types: strings, hashes, lists, sets",
            "OnMessage trigger for Redis pub/sub",
            "SSL/TLS connection support"
        ]
    }
}
