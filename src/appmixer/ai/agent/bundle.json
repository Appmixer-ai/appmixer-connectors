{
    "name": "appmixer.ai.agent",
    "engine": ">=6.4",
    "version": "1.2.0",
    "changelog": {
        "1.2.0": [
            "Fix Langfuse tracing: tool call spans were being silently dropped by @langfuse/otel v5 LangfuseSpanProcessor. The processor's isDefaultExportSpan filter requires a gen_ai.* attribute to pass. Added gen_ai.operation.name: 'execute_tool' and gen_ai.tool.name to all tool spans so they are correctly exported and appear in Langfuse traces."
        ],
        "1.1.0": [
            "Fix Langfuse tracing: tool call spans were not appearing in Langfuse traces.",
            "Root causes: (1) tool spans were parented to the wrong OTEL context because async-context propagation through the Vercel AI SDK was unavailable; fixed by capturing the agent span OTEL context synchronously in buildVercelTools and passing it explicitly to executeToolByName. (2) langfuse.observation.type was set to 'tool' which is not a valid Langfuse type; changed to 'span'. (3) Added @opentelemetry/context-async-hooks dependency for proper async context propagation."
        ],
        "1.0.0": [
            "First version."
        ]
    }
}