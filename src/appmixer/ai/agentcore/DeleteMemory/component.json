{
    "name": "appmixer.ai.agentcore.DeleteMemory",
    "description": "Permanently delete an Amazon Bedrock AgentCore Memory resource together with all of its records.",
    "author": "Appmixer <info@appmixer.com>",
    "version": "1.0.0",
    "auth": {
        "service": "appmixer:ai:agentcore"
    },
    "inPorts": [
        {
            "name": "in",
            "schema": {
                "type": "object",
                "properties": {
                    "memoryId": { "type": "string" }
                },
                "required": ["memoryId"]
            },
            "inspector": {
                "inputs": {
                    "memoryId": {
                        "type": "text",
                        "label": "Memory ID",
                        "index": 1,
                        "tooltip": "The identifier of the AgentCore Memory resource to delete. Deleting a memory also removes every event and memory record stored in it."
                    }
                }
            }
        }
    ],
    "outPorts": [
        {
            "name": "out",
            "schema": {
                "type": "object"
            }
        }
    ]
}
