{
    "name": "appmixer.zoho.books",
    "version": "1.1.1",
    "changelog": {
        "1.0.0": [
            "Initial version"
        ],
        "1.0.3": [
            "Improved error message when using Zoho account without organization."
        ],
        "1.0.4": [
            "Aligned CreateContact schema field names with inspector inputs for validation."
        ],
        "1.1.0": [
            "Added MakeApiCall component for performing arbitrary authorized API calls to the Zoho Books API."
        ],
        "1.1.1": [
            "Fixed intermittent \"Parse Error: Expected HTTP/\" (HPE_INVALID_CONSTANT) failures in components that read from Zoho. Every GET request was sent with an empty JSON body ({}), which Zoho does not read; on a reused connection those stray bytes corrupted the next response. This hit paginated reads hardest, because they issue many GET requests in a row. GET and HEAD requests are now sent without a body."
        ]
    }
}
